/**
 * Filter types config
 */

const all = {
  label: 'All',
  key: 'all'
}
const favorites = {
  label: 'Only Favorites',
  key: 'favorites'
}
const types = [
  all, favorites
]

export default types
export { all, favorites }
